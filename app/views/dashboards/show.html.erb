Dashboard#show

<div class="container">
    <h1>Your dashboard</h1>
    <p></p>
    <a class="btn btn-flat" href="#"></a>
  </div>
</div>
    <div class="container">
        <div class="row profile" display="flex">
        <div class="col-md-3">
          <div class="profile-sidebar">
            <!-- SIDEBAR USERPIC -->
            <%= current_user.id %>
            <div class="profile-userpic row justify-content-center" >
              <%#= cl_image_tag current_user.photo.key, crop: :fill if current_user.photo.attached? %>
    </div>
            <!-- END SIDEBAR USERPIC -->
            <!-- SIDEBAR USER TITLE -->
            <div class="profile-usertitle">
              <div class="profile-usertitle-name">
                <p><%# @user.email %></p>
              </div>
            </div>
            <!-- END SIDEBAR USER TITLE -->
            <!-- SIDEBAR BUTTONS -->

    <div class="profile-userbuttons">

              <%= link_to 'Available flights', watches_path, class:"btn btn-success btn-sm" %>

    </div>
     <div class="portlet light bordered">
                                                      <!-- STAT -->
        <div class="row list-separated profile-stat">

     </div>
                <!-- END STAT -->
     <div>
      <h4 class="profile-desc-title text-center">About Jason Davis</h4>
      <span class="profile-desc-text text-center m-3"> "Story of his life. Very interesting." </span>
      <div class="margin-top-20 profile-desc-link text-center m-3">
          <i class="fa fa-globe"></i>
          <a href="https://www.apollowebstudio.com">lewagon.com</a>
      </div>
     </div>
   </div>

 </div>
        <div class="col-md-9">
          <div class="profile-content">
             <div class="container mt-5">
                <h1>Mes réservations de vol</h1>

                <select id="trip-status-filter-btn">
                    <option value="#">select a trip status:</option>
                    <option value="pending">pending ⚫️</option>
                    <option value="confirmed">confirmed 🔵</option>
                </select>

            <%#
            <div class="dropdown">
              <button id="trip-status-filter-btn" class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              </button>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <a class="dropdown-item" href="#">pending ⚫️</a>
                <a class="dropdown-item" href="#">confirmed 🔵</a>
              </div>
            </div>
            %>

            <table class="table">
            <thead>
              <tr>
                <th scope="col">Statut</th>
                <th scope="col">Départ</th>
                <th scope="col">Destination</th>
                <th scope="col">Date de départ</th>
                <th scope="col">Date d'arrivée</th>
                <th scope="col">Nom du Jet</th>
                <th scope="col">Prix</th>

              </tr>
            </thead>
             <tbody>
                <% @bookings.each_with_index do |b, index| %>
              <tr class="<%= b.status %>">
                <!-- <th scope="row"> -->
                <!-- </th> -->
                <td>
                  <% if b.status == 'pending' %>
                    <%= "⚫️" %>
                  <% else %>
                    <%= "🔵" %>
                  <% end %>
                </td>
                <td><%= b.price_total%>€ </td>
                <td><%= b.start_date.strftime("%d/%m/%Y") %></td>
                <td><%= b.end_date.strftime("%d/%m/%Y") %></td>
                <td><%= b.booking_items.count %></td>
              </tr>
                <% end %>
            </tbody>
          </table>




          </div>
